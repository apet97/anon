# Dependencies & build output
node_modules/
dist/
coverage/

# Local runtime data (SQLite DB + WAL)
data/
*.db
*.db-wal
*.db-shm
*.log

# Secrets — never commit
.env
.env.*
!.env.example
.pumble*
tokens.json

# Local-only documentation (not committed)
how-it-was-built/
prompts/

# Session / agent state
.remember/
.claude/

# Editor / OS junk
.DS_Store
.idea/
.vscode/
*.swp
